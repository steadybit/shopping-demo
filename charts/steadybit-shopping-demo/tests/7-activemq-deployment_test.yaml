templates:
  - 7-activemq-deployment.yml
chart:
  appVersion: v0.0.0
tests:
  - it: manifest should match snapshot
    set:
    asserts:
      - matchSnapshot: { }
  - it: manifest should match snapshot replica count
    set:
      activemq:
        replicaCount: 2
    asserts:
      - matchSnapshot: { }